import SearchMenu from "components/SearchMenu";
import React from "react";

const Home = () => {
    return (
        <section className="home">
            <SearchMenu />
        </section>
    );
};

export default Home;
