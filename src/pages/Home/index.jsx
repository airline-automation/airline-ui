import React from 'react';
import SearchMenu from '../../components/SearchMenu';

const Home = () => {
    return (
        <section className="home">
            <SearchMenu />
        </section>
    );
};

export default Home;
