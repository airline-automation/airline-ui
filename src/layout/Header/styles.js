import styled from 'styled-components';

export const StyledHeader = styled.header`
    background-color: #ddd;
    color: #232323;
    height: 150px;

    .navbarListItem {
        cursor: pointer;
    }
`;
